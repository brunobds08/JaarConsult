import React from "react";

export default function ConsultaFipe(props) {

    const dadosfipe = {
        _valor: props.valor,
        _marca: props.marca,
    }

    return (
        <div className="dados-retornados-container">
            
        </div>
    )
}